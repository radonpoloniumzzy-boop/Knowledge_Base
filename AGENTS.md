# Knowledge Base Agent Guide

## Agent skills

### Issue tracker

项目使用本地 Markdown 跟踪需求、规格和开发任务。参见 `docs/agents/issue-tracker.md`。

### Triage labels

项目使用 Matt Skills 默认的五种任务状态。参见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单一上下文结构：根目录 `CONTEXT.md` 与 `docs/adr/`。参见 `docs/agents/domain.md`。
